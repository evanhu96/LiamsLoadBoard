// connect to backend test
import { GET_LOADS } from "../utils/queries";
import { useQuery } from "@apollo/client";
const Test = ({ rows }) => {
//   const { error, data } = useQuery(GET_LOADS);
//   console.log(data);
  return;
};

export default Test;
